# Overview Hover Select Extension for Gnome
Small GNOME extension that allows the user to select the window the cursor is hovering over upon closing the overview. This is similar to how Mission Control works on macOS. 
